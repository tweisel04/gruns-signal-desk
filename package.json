{
  "name": "gruns-signal-desk",
  "version": "1.0.0",
  "description": "Consumer sentiment & demand forecasting dashboard — a sample product built for Grüns",
  "scripts": {
    "dev": "npx serve . -p 3000",
    "build": "echo 'Static site — no build step' && exit 0"
  },
  "private": true
}
